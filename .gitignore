docs/superpowers/specs/
archived/
public/assets/*.mov

# Astro / Node
node_modules/
dist/
.astro/

# Env — WP API URL etc. Real values live in Vercel dashboard.
.env
.env.production

# Internal — never publish. This repo is PUBLIC.
# The rate card carries client names, what each actually paid vs list,
# a named prospect's discount, and an invoice number.
todo/services-and-pricing-draft.html
.claude/
