docs/superpowers/specs/
archived/
public/assets/*.mov

# Astro / Node
node_modules/
dist/
.astro/

# Env — WP API URL etc. Real values live in Vercel dashboard.
.env
.env.production

# Internal — never publish. This repo is PUBLIC.
# Pricing, client names, and anything showing what a client actually paid
# live in Drive at 02-Business/TechPersonaStudio/pricing/, never here.
# These patterns are a backstop in case one is ever dropped in by mistake.
todo/*pricing*
todo/*rate-card*
.claude/
